# Skaylink API clients libraries

Provides shared laravel-based client libraries

Extend repositories section and require `skaylink/cmapi-client` in your `composer.json`.

```
  "repositories": [
    {
      "type": "vcs",
      "url": "git@github.com:skaylink/clients.git"
    }
  ],
```
